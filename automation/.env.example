# Pipeline Guardian — todas as variáveis são OPCIONAIS.
# Sem OPENAI_API_KEY, o agente usa o classificador determinístico e ainda
# produz um diagnóstico válido (usedFallback: true).
#
# Copie para `automation/.env` (ignorado pelo git) e preencha só o que precisar.
# NUNCA coloque valores reais neste arquivo de exemplo.

# --- Modelo -----------------------------------------------------------------
#
# OPENAI_API_KEY é a ÚNICA variável necessária para ligar o modelo. É a única
# que é SEGREDO: nunca vai para .env.example, para o YAML, para uma GitHub
# Variable, para um artefato ou para o Railway.
OPENAI_API_KEY=

# As demais são CONFIGURAÇÃO (não sensíveis) e todas têm padrão no código
# (`automation/src/openai-config.mjs`). Deixe comentado para usar o padrão.

# Modelo da automação. Padrão: gpt-5-mini.
OPENAI_MODEL=gpt-5-mini

# Esforço de raciocínio: minimal | low | medium | high. Padrão: low.
# Valor não suportado cai no padrão e vira uma limitação declarada no relatório.
OPENAI_REASONING_EFFORT=low

# Teto da saída, em tokens (256 a 32000). Padrão: 2000. Inclui os tokens de
# raciocínio — apertar demais faz a resposta voltar `incomplete` e o agente cair
# no fallback.
OPENAI_MAX_OUTPUT_TOKENS=2000

# Tempo máximo da chamada, em ms (1000 a 300000). Padrão: 45000.
OPENAI_TIMEOUT_MS=45000

# Novas tentativas do SDK em erro transitório (0 a 3). Padrão: 1.
OPENAI_MAX_RETRIES=1

# --- Teste real da integração (opt-in) --------------------------------------
# `npm run openai:smoke` só chama a API de verdade com esta variável em `true`.
# Deixe fora do .env e passe na hora:
#
#   OPENAI_LIVE_TEST=true npm run openai:smoke
#
# OPENAI_LIVE_TEST=false

# --- Contexto do pipeline (opcional; preenchido pelo CI quando existir) ---
# GITHUB_REPOSITORY=senai/copa-figurinhas
# GITHUB_REF_NAME=feat/minha-branch
# GITHUB_SHA=
# GITHUB_EVENT_NAME=pull_request
# GITHUB_RUN_ID=
# PR_NUMBER=42

# Ambiente alvo: staging | production. Qualquer outro valor exige aprovação humana.
DEPLOY_ENVIRONMENT=staging
# Rollback de production sempre exige aprovação humana.
DEPLOY_ROLLBACK=false

# Comandos do pipeline analisados no modo real (separados por vírgula).
# AGENT_PIPELINE_COMMANDS=npm run lint,npm run test,npm run build

# Base do diff analisado (padrão: HEAD, ou seja, o working tree).
# AGENT_DIFF_BASE=origin/main

# --- Comentário na Pull Request ---
# Publicar exige opt-in explícito. Sem isto, o agente só grava
# reports/pr-comment.md e NÃO chama a API do GitHub.
AUTOMATION_ALLOW_PR_COMMENT=false
# GITHUB_TOKEN=

# --- Validação pós-deployment (smoke test + diagnóstico) ---
# URL pública da aplicação já implantada. No GitHub isto é a variável de
# repositório APP_BASE_URL — nunca um valor fixo no YAML.
# APP_BASE_URL=https://copa-figurinhas-production.up.railway.app

# Espera pela nova versão: 30 × 10s = até 5 minutos. Sempre finito.
SMOKE_MAX_ATTEMPTS=30
SMOKE_INTERVAL_SECONDS=10
SMOKE_REQUEST_TIMEOUT_MS=10000

# Commit que deve estar no ar. Enquanto /api/health reportar outro commit
# conhecido, o smoke test entende que a release antiga ainda está servindo e
# continua tentando. Preenchido pelo workflow a partir do commit do CI.
# SMOKE_EXPECT_COMMIT_SHA=

# Onde o smoke test grava o que observou — e de onde o diagnóstico lê.
# SMOKE_LOG_FILE=reports/deployment.log
# SMOKE_RESULT_FILE=reports/smoke-test.json
