# Aplicação ChatOps (FastAPI → Discord).
# Instalação:  python3 -m venv chatops/.venv && chatops/.venv/bin/pip install -r chatops/requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
httpx>=0.27,<1.0

# Testes
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
